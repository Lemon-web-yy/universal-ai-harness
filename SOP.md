# AI Coding 协作 SOP——Agent 使用原理与操作指南

> **定位：** 面向软件工程师的 Agent 协作开发标准操作程序（SOP）。前半部分讲原理与通用方法（不依赖任何特定框架），后半部分讲如何使用本仓库（universal-ai-harness）作为现成实现。
> **读者假设：** 具备工程能力，了解 LLM 基本用法，未系统使用过 Agent 编程。

## 1. 概述

当 Agent（具备终端执行、文件读写能力的 AI）进入代码库后，开发协作的瓶颈从"写代码的速度"转移到"结果的可信度"。本 SOP 的目标可以概括为一句话：

**将"信任 Agent"转化为"验证 Agent"——用可判定的流程约束替代对模型能力的盲目依赖。**

全文组织：

- 第 2 章建立风险模型：Agent 有哪几类固有风险
- 第 3 章给出四项核心机制：每项先讲原理依据，再讲操作方法
- 第 4 章是通用上手路径：不安装任何框架即可落地
- 第 5 章是本仓库的使用方法：将上述 SOP 实体化为可注入的仓库结构
- 第 6 章回答常见问题

## 2. 基础概念

### 2.1 AI 辅助编程的三种形态

| 形态 | 交互方式 | 责任边界 | 能力上限 |
|------|---------|---------|---------|
| 补全 | 编辑器内按光标续写 | 人逐行审查，责任在人 | 单点代码生成 |
| 对话 | 人工粘贴代码到聊天窗口 | 人工搬运上下文与结果 | 单次问答内的方案生成 |
| Agent | 授予终端、文件读写、命令执行权限 | Agent 自主执行多步操作 | 端到端完成需求（含阅读、修改、验证） |

前两种形态中人是执行主体，AI 是辅助工具；第三种形态中 Agent 成为执行主体。能力上限提高的同时，风险敞口同步扩大——Agent 是唯一能自主"完成工作"的形态，也是唯一能自主"造成破坏"的形态。本 SOP 针对第三种形态。

### 2.2 Agent 的三类固有风险

这三类风险源于当前 LLM 的工作机制，与模型能力强弱无关，换更强的模型不会消除：

**① 输出随机性。** 相同的需求输入两次，产出的方案结构可能不同。Agent 没有跨 session 的"一贯做法"，每次执行都存在自由发挥空间。

**② 上下文不持久。** Agent 做出的技术决策只存在于当前对话中。会话结束后决策即丢失，后续接手的 Agent（或同一 Agent 的下一次会话）需要从零重建对项目现状的理解，已达成一致的决策可能被重新讨论甚至推翻。

**③ 索引漂移。** Agent 维护的导航类文档（索引、清单、架构说明）会随代码演进逐渐落后于实际状态。真实案例：项目根文档登记"契约 001-004"，实际档案已积累至 007，后续 Agent 按索引检索档案失败。

### 2.3 SOP 的应对思路

上述风险的共同点：**均为可信度问题而非能力问题。** 对应的治理思路不是"限制 Agent 能力"，而是建立四项机制，使可信度不再依赖单次执行的运气：

| 风险 | 对应机制 | 章节 |
|------|---------|------|
| 输出随机性 | 角色分离 + Scope 边界 | 3.1 |
| 上下文不持久 | 档案先行 | 3.2 |
| 索引漂移 | 机械自检 | 3.4 |
| （流程成本失控） | 流程配重 | 3.3 |

## 3. 核心机制与原理

### 3.1 角色分离

**原理。** 让同一个 Agent 同时承担设计、实现、自审三种职责时，自审环节存在系统性偏置：它会验证"我是否完成了打算做的事"，而不是质疑"我打算做的是否正确"。设计与实现的正确性检查需要独立的视角，因此审核者不能是被审核者。

**操作方法。** 将职责拆分为三个角色，各自以独立的定义文件存在：

| 角色 | 职责 | 明确不做 |
|------|------|---------|
| Designer | 设计方案，产出设计档案 | 写代码、审核 |
| Reviewer | 审核设计与实现 | 设计、写代码 |
| Builder | 按设计档案的 Scope 实现 | 设计、审核 |

关键约束在 Builder 的定义中：**只实现 Scope 清单内列出的改动，清单外一律不做。** Agent 最常见的失控形态不是"写错代码"，而是"超出需求范围的自主发挥"——顺手调整格式、添加自认为更好的抽象、重构无关代码。Scope 清单（精确到文件/函数级）就是对此的机械约束。

### 3.2 档案先行

**原理。** 决策若只存在于对话中，等同于不存在。将决策写入版本库中的文档，有三个作用：跨会话持久（对抗风险②）、可供后续 Agent 检索引用、可被人类审计。因此规定：**架构调整、接口变更，先落档案文件，再动代码。**

**操作方法。** 模块内部决策使用精简 ADR 格式：

```markdown
# NNN_<kebab-case-english-name>.md
状态：Proposed

## Context        — 需求来源、痛点、约束
## Decision       — 技术决定，含选型理由（不只是"决定用 X"）
## Consequences   — 正向收益、已知技术债及归属
## Scope          — 改动清单，精确到文件/函数（Builder 的执行边界）
```

横跨多个模块的变更，档案升级为**契约**，在 ADR 基础上增加两个关键章节：

- **接口契约**——边界上的消息格式、字段定义、调用约定，写到两端可以独立实现后对账的粒度
- **不做的事**——逐条列出本次明确不实现的内容，**每条附理由**

"不做的事"是契约中最重要的章节：每明确一条"不做"，Agent 的自由发挥空间就收窄一处。跨模块协作的典型失败不是双方未完成约定事项，而是各自实现了未约定的事项。

**状态管理。** 档案状态字段为 `Proposed → Accepted → Deprecated`。Agent 产出后只能标记 Proposed；**翻转为 Accepted 必须由人在真实环境验证后执行。** 验证者拥有拍板权，Agent 不得代替。

### 3.3 流程配重

**原理。** 流程本身有成本（时间、上下文占用、协作开销）。当流程成本超过其防范的风险时，理性参与者会开始绕过流程，SOP 整体失效。因此流程重量必须与需求的风险等级匹配——这是 SOP 能够长期执行的前提，而非可选项。

**操作方法。** 按"需求牵连的模块数 + 是否需要接口同步"两个可判定条件评级：

| 评级 | 判定条件 | 流程 |
|------|---------|------|
| L0 | 单模块内部改动 | 模块内三角色闭环，不启动跨模块流程 |
| L1 | 接口需同步给其他模块 | 只编写并发布契约，各模块自行实现 |
| L2 | 跨 2-3 个模块的功能集成 | 契约 + 并行派发 + 架构审核 + 收尾 |
| L3 | 牵动全部模块的大型变更 | 全程编排的完整流程 |

执行原则：**能 L0 不 L1，能 L1 不 L2。** 日常需求的绝大多数是 L0。评级机制的价值不在于把需求推入重流程，而在于给"何时必须停下来写契约"提供机械判定标准（涉及 2+ 模块且需接口同步）。

一条可泛化的原则：**SOP 条款必须可机械判定。** 需要"视情况而定"的条款最终都会被绕过。

### 3.4 机械自检

**原理。** 前三项机制依赖 Agent 执行，但"应该做"与"每次都做"之间存在执行缺口。文档约定的遵从度是软约束；脚本检查的遵从度是硬约束——脚本不会疲劳、不会遗漏、不会因"本次改动很小"而放宽。Agent 对脚本的服从度显著高于对文档规则的服从度，因为脚本输出的是可判定的结果而非建议。

**操作方法。** 将关键纪律固化为脚本，**收尾归档前强制执行**，检查项至少覆盖：

- 导航索引与实体文件双向对账（索引登记的编号有对应文件；存在的文件已登记）
- 档案编号无冲突、无跳号
- 模板占位符无残留（项目定制完成的标志）
- 核心文档体量未超标（防止入口文档膨胀为内容仓库）

问题分级处理：**P0 必须修复后才能收尾；P1 本轮内修复；P2 登记后可延后。**

## 4. 上手路径（通用方法）

本章不依赖任何框架，可在任何项目立即落地。以 Claude Code 为例（机制在同类 Agent 工具中通用）。

### 4.1 环境准备

安装 Claude Code CLI 并完成认证，方法参见官方文档（`https://docs.claude.com/en/docs/claude-code`，安装方式随版本演进，以官方为准）。在项目根目录启动后即可对话。

### 4.2 编写项目 CLAUDE.md

CLAUDE.md 是 Agent 进入项目后读取的第一个文件，作用是声明项目上下文与硬规则，替代每次会话的人工说明。最小可用模板：

```markdown
# <项目名> — 一句话定位

## 项目概述
做什么、服务谁、当前形态（单体/微服务/库）。

## 目录结构
src/          — （一句话职责）
tests/        — （一句话职责）

## 构建与测试命令
- 构建：npm run build
- 测试：npm test

## 硬规则（违反即返工）
1. 依赖方向单向，禁止逆向引用
2. 新增依赖需说明理由
3. 修改公共接口前先通知使用方
```

编写要点：只放 Agent 高频需要的路由信息；架构细节下沉到专门文档；行数控制在 120 行以内，超标说明内容该下沉了。

### 4.3 定义三个角色

在 `.claude/agents/` 下为每个角色创建一个定义文件。格式示例（designer.md）：

```markdown
---
name: designer
description: 架构设计者 — 根据需求设计架构方案，产出设计档案草稿
---

# Designer 角色

## 职责
1. 读取 docs/ARCHITECTURE.md 了解现有架构
2. 读取 docs/core-beliefs.md 了解硬规则
3. 按 ADR 模板产出 docs/design-docs/NNN_*.md（状态 Proposed）

## 不做
写代码、审核（分别归 builder / reviewer）
```

reviewer.md 与 builder.md 同构：职责 + 不做 + 关键纪律（Reviewer 附审核清单；Builder 强调"只实现 Scope 内改动"与"无法验证时标注 `[未验证]`，不得谎称已验证"）。

### 4.4 执行一个完整需求闭环

角色就位后，单个需求（L0）的标准执行序列：

| 步骤 | 执行者 | 操作 |
|------|--------|------|
| 1 | 人 | 向 Designer 描述需求，提供相关文件路径（不粘贴代码全文） |
| 2 | Designer | 产出设计档案（Context / Decision / Consequences + Scope） |
| 3 | Reviewer | 按清单审核：格式完整、Scope 精确、不违反硬规则 |
| 4 | 人 | **审阅设计档案并确认**（spec review gate，此为架构决策点） |
| 5 | Builder | 按 Scope 实现，本地编译/测试验证；无法验证的标注 `[未验证]` |
| 6 | Reviewer | 复审实现与 Scope 的一致性 |
| 7 | 人 | 真实环境测试通过后，档案状态翻转为 Accepted |

两个纪律贯穿全程：**给路径不给全文**（档案是单一真相源，粘贴会引入版本不一致）；**验证结果随代码一起交付**（没有验证标注的交付不予接受）。

### 4.5 建立收尾自检

将 3.4 节的检查项落为脚本（一个百行以内的 Python/Shell 脚本即可），在每次收尾前运行。最低限度先做人工核对：文档中提及的每个档案编号、文件路径、命令，与仓库实际状态一致。

## 5. 使用 universal-ai-harness

### 5.1 适用时机

第 4 章的方法需要在每个项目手工搭建。当出现以下信号时，建议改用本仓库的一键注入：

- 在第二个、第三个项目重复编写同样的角色定义与文档骨架
- 团队多人协作，需要统一的治理结构
- 项目进入多模块阶段，出现跨模块契约协调需求

### 5.2 安装与初始化

```bash
# 单模块项目（三角色，模块自治）
python harness_installer.py --target /path/to/your_project --profile single

# 多模块项目（增加 coordinator + architect-reviewer + 跨模块契约流程）
python harness_installer.py --target /path/to/your_project --profile multi

# 进入项目执行自检，应显示 [OK]
cd /path/to/your_project
python .harness/audit_harness.py
```

注入内容：`.claude/`（角色 + 技能 + 命令）与 `.harness/` 治理单一命名空间（`contracts/` 契约体系、`docs/` 知识库与项目画像模板、`progress.md` 进度账本、`audit_harness.py` 自检脚本），以及按 profile 生成的根级 CLAUDE.md（顶部带初始化横幅）。

**注入后必做的三件事：**

1. 执行 `/project-intake`——agent 扫描 README / 目录结构 / 构建配置，生成 `.harness/docs/PROJECT.md` 项目画像并改写根级 CLAUDE.md 的项目上下文段、消除初始化横幅（横幅残留为 audit P1：画像不采集完无法收尾）。这一步是 4.2 节"CLAUDE.md 声明项目上下文"的自动化落地
2. 改写 `.harness/docs/ARCHITECTURE.md`——按实际模块填写边界、数据流、构建体系
3. （multi 形态）用 harness-setup 流程为每个模块落地独立治理结构

### 5.3 日常工作流

注入后，日常需求按路由表执行：

| 需求特征 | 评级 | 流程入口 |
|---------|------|---------|
| 单模块内部 | L0 | 模块内 Designer → Reviewer → Builder（4.4 节闭环） |
| 接口同步 | L1 | 编写契约发布至 .harness/contracts/，各模块自行实现 |
| 跨 2-3 模块 | L2 | cross-module-change 流程：契约 → 确认 → 并行派发 → 架构审核 → 收尾 |
| 全模块级 | L3 | 同 L2，Coordinator 全程主导 |

所有收尾统一执行 `python .harness/audit_harness.py`。

## 6. 常见问题

**Q：注入会覆盖现有文件吗？**
A：治理文件全部收在 `.harness/` 单一命名空间，不触碰项目自有目录（`docs/`、`tools/`、`contracts/` 等完全归项目支配，卸载 = 删 `.harness/` + `.claude/` + `CLAUDE.md`）。唯一例外：根级 `CLAUDE.md`——注入会覆盖它，目标已有时请先备份。

**Q：single 与 multi 可以互相切换吗？**
A：可以。用另一个 profile 重新注入即可，会补齐对应形态的角色与技能。

**Q：支持哪些语言？**
A：全部。治理机制与语言无关；自检脚本的占位符检查已覆盖 `.py/.rs/.js/.ts/.go/.java/.c/.h/.cpp` 等常见扩展名。

**Q：不用 Claude Code 还能用这套 SOP 吗？**
A：第 2、3 章的原理与机制是工具无关的，可迁移到任何支持子代理/角色定义的 Agent 工具；第 4、5 章的具体配置格式以 Claude Code 为例。

**Q：为什么 Agent 写的档案状态只能停在 Proposed？**
A：Accepted 意味着"已在真实环境验证通过"，这是人类决策权。允许 Agent 自行翻转状态，等于允许它替代你验收。
