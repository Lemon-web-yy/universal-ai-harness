# PROJECT — {{PROJECT_NAME}} 项目画像

*日期：YYYY-MM-DD*
*状态：Proposed*

> **定位：** 项目"是什么 / 怎么跑 / 约束是什么"的权威档案。CLAUDE.md 之后的第二读物。
> **生成：** 由 `/project-intake` 起草（agent 扫描 README / 目录 / 构建配置），人工校对后转 Accepted。
> **体量红线：** ≤120 行；超出说明细节该下沉（架构细节 → `.harness/docs/ARCHITECTURE.md`，决策 → `.harness/docs/design-docs/`）。

## 1. 项目定位

一段话：做什么、服务谁、当前形态（单体 / 微服务 / 库 / 固件 / …）。

## 2. 技术栈与运行环境

| 维度 | 内容 |
|------|------|
| 语言 / 框架 | （含版本） |
| 运行 OS / 部署目标 | |
| 关键硬件 | （无则删行） |

## 3. 目录结构导读

| 目录 | 职责（一句话） |
|------|---------------|
| src/ | （按项目实际替换） |

> 只列项目自身目录；`.harness/` 为治理框架（见 CLAUDE.md）。

## 4. 构建与运行

| 操作 | 命令 | agent 可否执行 |
|------|------|----------------|
| 构建 | | ✅ / ❌ |
| 测试 | | ✅ / ❌ |
| 部署 / 运行 | | ✅ / ❌ |

## 5. 外部依赖与接口

- 依赖的库 / 服务 / 设备：
- 对外暴露的接口（API / 消息 / 文件）：

## 6. 硬约束（违反即返工）

从 README / 代码规范 / 实践中提炼，每条能回答"防止了什么失败"：

1. （示例）依赖方向单向，禁止逆向引用

---

**状态流转：** Proposed（agent 起草）→ Accepted（用户校对拍板）→ 随架构演进更新（大改走 `.harness/docs/design-docs/` 档案）
