# NNN — <决策标题>（<kebab-case-english-name>）

*日期：YYYY-MM-DD*
*状态：Proposed*

> **定位：** 模块内部架构决策档案（精简 ADR）。触发：架构调整 / 接口变更。流程：先写本档案再动代码。
> 跨模块变更不走本模板——使用 `.harness/contracts/_template.md`（契约 5 章节）。

## Context

### 需求来源

<哪个需求 / 哪个契约触发了本决策>

### 痛点

<现状存在什么问题，不解决会怎样>

### 约束

<技术约束 / 既有架构约束 / 相关档案编号>

## Decision

<决定怎么做。含选型理由——不只是"决定用 X"，还要写清为什么不用 Y。>

## Scope（改动清单 —— Builder 的执行边界）

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `<path>` | 新建/修改/删除 | <改什么，精确到函数> |

> 清单外一律不改。Builder 据此实现，Reviewer 据此核对。

## Consequences

### 正向

<带来了什么收益>

### 负向 / 待办

<已知技术债，归属哪个未来档案>

## 验证

- [ ] 编译验证：<方式；无法执行时标 `[未验证]`>
- [ ] 测试验证：<单测 / 集成测试>
- [ ] 真实环境验证（用户做）

---

**状态流转：** Proposed（agent 写完）→ Accepted（用户真实环境验证后拍板）→ Deprecated
